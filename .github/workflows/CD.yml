name: Main Workflow

on:
  push:
    branches: [ feat/#39-distribution ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout all submodules
        uses: actions/checkout@v3
        with:
          submodules: true

      - name: Run module workflows
        run: |
          echo "Triggering coupon workflow..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://github.com/Sparta-Triple/CultureTicket/com.culture-ticket.client.coupon/.github/workflows/coupon-github-action.yml/dispatches \
            -d '{"ref":"feat/#39-distribution"}'

          echo "Triggering gateway workflow..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://github.com/Sparta-Triple/CultureTicket/com.culture-ticket.client.coupon/.github/workflows/gateway-github-action.yml/dispatches \
            -d '{"ref":"feat/#39-distribution"}'

          echo "Triggering gateway workflow..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://github.com/Sparta-Triple/CultureTicket/com.culture-ticket.client.coupon/.github/workflows/performance-github-action.yml/dispatches \
            -d '{"ref":"feat/#39-distribution"}'
          
          echo "Triggering gateway workflow..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://github.com/Sparta-Triple/CultureTicket/com.culture-ticket.client.coupon/.github/workflows/queue-github-action.yml/dispatches \
            -d '{"ref":"feat/#39-distribution"}'
          
          echo "Triggering gateway workflow..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://github.com/Sparta-Triple/CultureTicket/com.culture-ticket.client.coupon/.github/workflows/reservation-github-action.yml/dispatches \
            -d '{"ref":"feat/#39-distribution"}'
          
          echo "Triggering gateway workflow..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://github.com/Sparta-Triple/CultureTicket/com.culture-ticket.client.coupon/.github/workflows/ticket-github-action.yml/dispatches \
            -d '{"ref":"feat/#39-distribution"}'
          
          echo "Triggering gateway workflow..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://github.com/Sparta-Triple/CultureTicket/com.culture-ticket.client.coupon/.github/workflows/user-github-action.yml/dispatches \
            -d '{"ref":"feat/#39-distribution"}'
          
          echo "Triggering gateway workflow..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://github.com/Sparta-Triple/CultureTicket/com.culture-ticket.client.coupon/.github/workflows/server-github-action.yml/dispatches \
            -d '{"ref":"feat/#39-distribution"}'
          
